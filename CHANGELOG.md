# 更新日志

## [1.0.8] - 2026-08

### Bug 修复
- 修复新版 Chrome（Chromium 126+）中下载书签报 "Bookmark id is invalid." 的问题
- 根因：新版 Chrome 为统一扩展标准提供了 `browser` 全局命名空间，原 `typeof browser !== 'undefined'` 判断将 Chrome 误判为 Firefox，导致按 Firefox 逻辑返回无效的根文件夹 ID
- 修复方案：浏览器类型改用 UA 作为权威判断（仅当 UA 含 `Firefox/` 且 `browser` 可用时才算 Firefox）；bookmarks API 选择统一封装并带防御性回退

## [1.0.7] - 2026-08

### Bug 修复
- 修复 Firefox 下载书签报 "Bookmark id is invalid." 的问题
- 根因：原实现硬编码 Firefox 根文件夹 ID（`toolbar_____` 等）并按标题映射，但部分 Firefox 环境（旧版本迁移等）根文件夹 ID 是随机 GUID，映射出的 ID 无效导致创建全部失败
- 修复方案：改为动态读取当前浏览器的真实根文件夹，按「节点原始 id → 标题精确匹配 → 类别别名 → 兜底」多策略识别导入目标
- 优化类别别名匹配：书签栏类排除"菜单"类，避免"书签栏"被误导入到"书签菜单"

## [1.0.6] - 2026-08

### 诊断与数据校验
- 上传/下载流程增加书签数量统计与数据摘要日志，方便定位同步问题
- 上传时检测本地书签为 0 会给出明确提示
- 下载时检测服务器数据为 0 会给出明确提示
- 导入后校验创建结果：任何节点创建失败都会明确报错并自动回滚备份
- 回滚过程增加完整性校验（恢复数量对比），恢复失败会输出详细诊断

## [1.0.5] - 2026-08

### Bug 修复
- 修复了下载书签后书签数量为 0 的问题（`clearAllBookmarks` 对书签节点误用 `removeTree` 导致抛错中断导入流程）
- 修复了根文件夹识别的大小写敏感问题（英文 Chrome 的 "Bookmarks bar" 小写无法匹配）
- 添加了导入失败时的自动备份回滚机制，防止清空书签后无法恢复

### 兼容性改进
- `clearAllBookmarks` 现在支持 Firefox 的 `browser.bookmarks` API
- 书签删除逻辑区分书签节点与文件夹节点，分别使用 `remove` / `removeTree`

## [1.0.3] - 2024-02

### 国际化支持
- 添加了完整的中英文界面支持
- 实现了语言选择的持久化存储
- 优化了默认语言的检测逻辑
- 使用 chrome.storage.sync 存储语言偏好

### 界面改进
- 添加了设置面板的展开/收起动画效果
- 优化了按钮的加载状态显示
- 改进了错误消息的显示方式
- 增强了用户操作的视觉反馈

### 构建系统
- 优化了扩展打包流程
- 添加了版本号自动同步功能
- 改进了构建过程的调试支持
- 优化了文件过滤规则

### 性能优化
- 改进了语言切换的性能
- 优化了存储访问逻辑
- 减少了不必要的DOM操作

### 已知问题修复
- 修复了语言切换后某些文本未更新的问题
- 修复了设置面板动画可能卡顿的问题
- 修复了某些错误提示显示不完整的问题

## [1.0.1] - 2024-01

### 安全性改进
- 改进了密码加密存储机制，使用 AES-GCM 加密算法
- 添加了密码长度验证（最少8个字符）
- 添加了服务器地址格式验证

### 错误处理优化
- 优化了 WebDAV 连接错误提示，增加了更详细的错误类型
- 改进了认证失败的错误提示
- 添加了网络连接失败的专门处理

### 数据存储改进
- 使用 chrome.storage.sync 存储用户配置，支持跨设备同步
- 添加了版本控制机制，为后续数据迁移做准备
- 优化了加密数据的存储格式，集成了 salt 和 IV

### 用户体验优化
- 添加了书签变更提醒功能
- 使用徽章显示未同步状态
- 优化了同步过程中的状态显示

### 其他改进
- 添加了基础的数据验证机制
- 改进了代码结构，增加了错误处理的一致性
- 优化了配置存储的安全性

### 已知问题
- WebDAV 连接仍使用基本认证方式
- 缺少自动同步功能
- 未实现同步冲突处理 